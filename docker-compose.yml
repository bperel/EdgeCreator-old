version: '2'
services:
  web:
    build: .
    ports:
     - "8001:80"
    volumes:
     - .:/var/www/html/EdgeCreator
     - ../edges:/var/www/html/DucksManager/edges
networks:
  default:
    external:
      name: dm_network
